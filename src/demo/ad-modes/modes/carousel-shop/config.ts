import type { AdModeDefinition } from '../../types'

// Carousel Shop — disabled stub. Concept on the roadmap; creative + JSON
// + scrubber UX not designed yet.
export const carouselShopMode: AdModeDefinition = {
  id: 'Carousel Shop',
  label: 'Carousel Shop',
  enabled: false,
}
