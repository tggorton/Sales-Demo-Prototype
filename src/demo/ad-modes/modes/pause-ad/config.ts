import type { AdModeDefinition } from '../../types'

// Pause Ad — disabled stub. Sales-team has the concept on the roadmap but the
// creative + compliance JSON aren't approved yet. Flip `enabled` to true and
// fill in the dhyh* fields once both exist.
export const pauseAdMode: AdModeDefinition = {
  id: 'Pause Ad',
  label: 'Pause Ad',
  enabled: false,
}
